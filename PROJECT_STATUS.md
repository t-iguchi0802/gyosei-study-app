# 行政書士2026 学習アプリ 開発引継ぎ

最終更新: 2026-09-25

## 1. リポジトリと公開先

- GitHub: `https://github.com/t-iguchi0802/gyosei-study-app`
- 基準ブランチ: `main`
- Firebase Hosting: `https://gyosei-study-2026-tiguchi.web.app/`
- Firebaseプロジェクト: `gyosei-study-2026-tiguchi`
- 第1回予想模試: 60問（完成済み。問題を作り直す段階ではない）

新しいCodexセッションでは、最初にこのファイル、`README.md`、直近コミット、`git status`、Firebase設定を確認すること。過去の会話を推測して実装しないこと。

## 2. 現在の完成状態

現在の公開版には以下が実装されている。

- 第1回予想模試60問
- 1問ずつ復習モード
- 60問の通し試験と一括採点
- Googleログイン
- Cloud FirestoreによるPC・スマホ間同期
- localStorage保存
- Firebase Hosting
- PWA用manifest・アイコン
- 公開URLのQRコード
- 全肢の理由
- ひっかけポイント
- 具体的な判例説明
- 周辺知識
- 解答テクニック

PWA用manifestはあるが、Service Workerによるアプリ本体のオフラインキャッシュは未実装。

## 3. 最後に完了した作業

Firebase公開版の `app.js`、`data.js`、`styles.css` が旧GitHub `main` より新しかったため、公開中の解説改善をGitHubへ保全した。

- 保全コミット: `53add56` `Preserve deployed explanation improvements`
- 完全バイト一致用コミット: `5aec6f4` `Preserve deployed data bytes exactly`
- `.gitattributes` で `data.js -text` を指定し、Gitの改行自動変換による差異を防止

2026-09-25確認時のSHA-256:

| ファイル | SHA-256 |
|---|---|
| `app.js` | `E36DA436CEE4931D50961454BF504A97EE1CC64CA7976DBCC2236C6C272E2A3B` |
| `data.js` | `7E437D413286077FC87856960CF6D8E01C88F7E1F22B3B0F448567368532FB38` |
| `styles.css` | `8165EC37DD27B7591507B5BF777B850A7B543FD511D2CBC73A02D35F82D5DDE2` |

上記3ファイルについて、GitHub Raw（コミット `5aec6f4`）とFirebase公開版が完全一致することを確認済み。

確認済み事項:

- JavaScript構文チェック合格
- 問題数60問
- 択一54問の全選択肢に理由あり
- 全肢解説・ひっかけ表示・判例表示ロジックあり
- 具体化された判例説明あり
- 解答テクニックあり
- 既存Playwrightスモークテスト合格
- スマホ390px、PC1280pxで横方向のはみ出しなし

この保全作業ではFirebase本番データ変更・本番デプロイを行っていない。

## 4. 現在のv1データ構造

localStorageキー:

```text
gyosei2026_mock1_learning_v1
```

Firestore保存先:

```text
users/{uid}/mockExams/mock1
```

概略:

```js
{
  version: 1,
  updatedAt,
  rounds: {
    1: round,
    2: round,
    3: round,
    4: round,
    5: round
  }
}
```

各回:

```js
{
  status: "new" | "in_progress" | "submitted",
  startedAt,
  submittedAt,
  examAnswers: {},
  records: {},
  writtenScores: {},
  updatedAt
}
```

問題ごとの記録:

```js
{
  answer,
  confidence: "confident" | "unsure" | "",
  status: "ok" | "ng" | "partial" | "pending",
  earned,
  max,
  source: "review" | "exam",
  answeredAt
}
```

### v1の制約

- 1回目から5回目までの固定方式
- 学習モードと通し試験が同じ `rounds`、`records` を共有
- 同じ回・同じ問題の再回答は上書きされる
- 復習モードの現在位置は保存されない
- 同期は1回答単位ではなく、更新時刻が新しい「回全体」を採用する
- PCとスマホで同じ回を同時操作すると、一方の更新を失う可能性がある

## 5. 承認済みの設計方針

### 学習モード

- 回数無制限
- 全回答履歴を保存し、5回を超えても削除しない
- 画面には各問題の直近5回を表示
- 通算回数、正解数、正答率を表示
- 全60問の周回数を自動管理
- 学習の中断位置を保存
- GoogleアカウントによるPC・スマホ同期
- 「続きから」「今日の復習」「苦手問題」「全60問」を用意

回答順序は必ず次の順とする。

```text
回答選択
→ 自信あり／迷った
→ 回答確定
→ 正誤・解説表示
```

正解表示後に自信度を選ばせない。次の4種類を区別して保存する。

- 正解＋自信あり
- 正解＋迷った
- 不正解＋迷った
- 不正解＋自信あり

「不正解＋自信あり」は思い込みによる重要な弱点として、最優先の復習対象にする。

### 通し試験

- 学習履歴と保存先を完全分離
- 回数無制限
- 各回の得点・全回答を保存
- 中断位置を保存
- PC・スマホ同期
- 試験中は正解、解説、学習履歴、過去成績を表示しない
- 終了後に一括採点

### 習得・復習判定

単純な通算正答率だけで判定しない。次を組み合わせる。

- 最新結果
- 連続正解数
- 直近5回
- 最新の自信度
- 不正解＋自信ありの有無

習得の基本条件:

```text
直近3回連続正解
かつ
最新回答が「自信あり」
```

判定値はコード内に散在させず、設定定数として集約する。

### 簡易間隔反復

14日固定にはしない。

- 初期は短い間隔で再出題
- 正解と自信ありが続けば段階的に間隔を延長
- 不正解、迷った、不正解＋自信ありで段階を下げる
- 試験日までの残り日数が短い場合は、長い間隔を自動で短縮
- ユーザーに複雑な設定操作を要求しない
- ホームでは自動計算した「今日の復習 ○問」を表示

初期実装では、段階ごとの基本間隔と試験日補正を定数化し、後から調整可能にする。

## 6. 計画中のv2データ構造

旧v1を上書きせず、別のv2保存先を使用する。

概要ドキュメント:

```text
users/{uid}/apps/gyosei2026Mock1
```

学習回答履歴:

```text
users/{uid}/apps/gyosei2026Mock1/learningAttempts/{attemptId}
```

通し試験履歴:

```text
users/{uid}/apps/gyosei2026Mock1/examSessions/{sessionId}
```

学習回答は上書き型ではなく、1回答1レコードの追記型とする。最低限、次を保存する。

```js
{
  questionId,
  cycle,
  learningMode,
  answer,
  result,
  confidence,
  misconception: result !== "ok" && confidence === "confident",
  answeredAt,
  clientId
}
```

通し試験は独立セッションとして、回答、得点、開始・終了時刻、現在位置を保持する。

## 7. v1移行方針

- 旧localStorageキーを削除しない
- 既存Firestore `users/{uid}/mockExams/mock1` を削除・初期化しない
- 復元可能な記録だけをv2へコピーする
- 安定した移行IDで重複移行を防止する
- `source: "review"` は学習履歴候補
- 提出済みの試験記録は通し試験履歴候補
- `status: "in_progress"` かつ回答ありは中断中試験候補
- 混在・矛盾していて安全に分類できない記録は旧履歴として保持する
- 移行完了マーカーは全処理成功後にだけ記録する
- v1で既に上書きされ、現データに残っていない過去回答は復元不能であることを明記する

Firebase本番データに対する移行は、移行処理とテストを完成させ、内容確認後に実施する。

## 8. ホーム画面の基本構成

スマホで「アプリを開く→1回押す→学習開始」を目標とする。

優先順:

1. 続きから学習（例: 3周目・問28/60）
2. 今日の復習 ○問
3. 苦手問題 ○問
4. 全60問を学習
5. 通し試験
   - 新しく開始
   - 中断中なら問34/60から再開
6. 学習状況
   - 習得
   - 要復習
   - 未定着

固定の「1回目～5回目」選択は廃止する。情報を詰め込みすぎず、最も必要な学習開始操作を上部に置く。

## 9. 現在作業中・未完了

公開版の保全とGitHub一致確認までは完了。

以下はまだ未実装。

- v2保存構造
- v1からv2への移行処理
- 学習モードと通し試験の保存分離
- 無制限の学習履歴
- 周回管理と学習再開
- 直近5回・通算統計
- 自信度を回答確定前に必須化
- 思い込み誤答の優先復習
- 簡易間隔反復と「今日の復習」
- 無制限の通し試験履歴と再開
- 新ホーム画面
- v2用テスト

## 10. 次に行う作業

次の実装フェーズでは、いきなり本番データを変更しない。

1. v2の定数、純粋な判定関数、データ型を設計
2. v1移行テスト用の固定データを作成
3. ローカルv2保存と移行処理を実装
4. 学習モードの追記型履歴、周回、再開を実装
5. 自信度確定前フローと復習優先度を実装
6. 今日の復習と簡易間隔反復を実装
7. 通し試験を独立セッション化
8. Firestore同期を実装
9. スマホ中心のホーム画面へ更新
10. 自動テストとPC・スマホ実機同期を確認
11. `README.md` と本ファイルを更新
12. ユーザー確認後にだけ本番デプロイ・本番移行を行う

## 11. 注意事項

- `app.js`、`data.js`、`styles.css` の公開版保全内容を巻き戻さない
- 全肢の理由、ひっかけポイント、判例の具体的説明、周辺知識、解答テクニックを失わない
- 問題60問を作り直さない
- GitHubはコード管理用であり、ユーザー履歴をGitHubへ保存しない
- Firebase本番データを勝手に削除・初期化しない
- 本番デプロイ前に、GitHubへのpushだけでなく公開URLの実表示を確認する
- 重要な作業完了ごとにこのファイルを更新する
